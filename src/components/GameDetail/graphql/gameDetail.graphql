#import '../../../graphql/fragments/gameDetailComment.graphql'

query GameDetail($gameId: ID!, $commentsLimit: Int!) {
    gameById(gameId: $gameId) {
        id
        name
        players
        menRole
        womenRole
        bothRole
        hours
        days
        year
        web
        galleryURL
        photoAuthor
        description
        averageRating
        amountOfRatings
        amountOfPlayed
        currentUsersRating {
            id
            rating
            state
        }
        currentUsersComment {
            id
            comment
        }
        labels {
            id
            name
            description
        }
        ratingStats {
            count
            rating
        }
        video {
            id
            path
        }
        authors {
            id
            person {
                name
                nickname
            }
        }
        groupAuthor {
            id
            name
        }
        commentsPaged(offset: 0, limit: $commentsLimit) {
            totalAmount
            comments {
                ...gameDetailComment
            }
        }
        similarGames {
            id
            name
            averageRating
            amountOfRatings
            year
        }
        gamesOfAuthors {
            id
            name
            averageRating
            amountOfRatings
            year
        }
        events {
            id
            name
            from
            to
        }
    }
}