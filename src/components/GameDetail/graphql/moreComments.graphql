#import './fragments.graphql'

query MoreComments($gameId: Int!, $commentsOffset: Int!, $commentsLimit: Int!) {
    gameById(id: $gameId) {
        id
        commentsPaged(offset: $commentsOffset, limit: $commentsLimit) {
            comments {
                ...gameDetailComment
            }
            totalAmount
        }
    }
}