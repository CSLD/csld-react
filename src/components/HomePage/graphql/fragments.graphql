#import "src/graphql/fragments/baseGameData.graphql"

fragment baseCommentData on Comment {
    id
    commentAsText
    added
    game {
        ...baseGameData
    }
    user {
        id
        person {
            name
            nickname
        }
        image {
            id
            path
        }
    }
}
