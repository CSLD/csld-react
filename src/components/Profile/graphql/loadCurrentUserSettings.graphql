query LoadCurrentUserSettings {
    loggedInUser {
        id
        amountOfPlayed
        amountOfCreated
        person {
            email
            name
            nickname
            birthDate
            city
        }
    }
}