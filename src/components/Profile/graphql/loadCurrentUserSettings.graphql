query LoadCurrentUserSettings {
    loggedInUser {
        id
        amountOfPlayed
        amountOfCreated
        image {
            id
        }
        person {
            email
            name
            nickname
            birthDate
            city
        }
    }
}