#import "src/graphql/fragments/baseGameData.graphql"

query SearchGames($searchTerm: String! $limit: Int!) {
    games {
        bySearchTerm(offset: 0, limit: $limit, searchTerm: $searchTerm) {
            ...baseGameData
        }
    }
}